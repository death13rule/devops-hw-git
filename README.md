# devops-hw-git
Будут проигнорированы:
1. Директория .terraform 
2. Файлы, содержащие в имени следующие строки: .tfstate, *.tfvars, *.tfvars.json, override.tf, override.tf.json, *_override.tf, *_override.tf.json, .terraform.tfstate.lock.info, .terraformrc, terraform.rc, crash.log, crash.*.log 
